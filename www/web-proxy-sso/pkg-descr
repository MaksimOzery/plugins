Authentication via Kerberos allows the Single Sign On (SSO) mechanism for domain
users. In this mode, the user authenticates once, at the time of login in
domain and in the future, when authenticating on a proxy server, login and password
will not be asked.
